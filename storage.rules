rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Report assets / screenshots are stored under the owner's uid.
    match /audits/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
